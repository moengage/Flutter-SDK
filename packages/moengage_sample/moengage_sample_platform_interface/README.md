# moengage_sample_platform_interface

A common platform interface for the [`moengage_sample`](../moengage_sample) plugin.

This interface allows platform-specific implementations of the `moengage_sample`
plugin, as well as the plugin itself, to ensure they are supporting the
same interface.

Note: This is a reference/scaffold module, not intended for production use.
