group 'com.moengage.flutter_personalize'
version '1.0'

buildscript {
    ext.kotlin_version = '1.9.23'
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url "https://central.sonatype.com/repository/maven-snapshots/" }
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:8.13.2'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath "com.moengage.android.hybrid.module.config.plugin:com.moengage.android.hybrid.module.config.plugin.gradle.plugin:0.0.4"
        // classpath "org.jlleitschuh.gradle:ktlint-gradle:12.1.1"
    }
}

rootProject.allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url "https://central.sonatype.com/repository/maven-snapshots/" }
    }
}

ext {
    moengageNativeBomVersion = "2.2.1"
    moengagePluginBaseBomVersion = "2.1.0-SNAPSHOT"
}

apply plugin: 'com.moengage.android.hybrid.module.config.plugin'
// apply plugin: 'org.jlleitschuh.gradle.ktlint'

hybridModuleConfig.configurePlugin {
    kotlinOptions {
        enableJvmTarget = true
        compilerArgs = []
    }
}

android {
    namespace "com.moengage.flutter.personalize"
    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
    }
}

dependencies {
   implementation(platform("com.moengage:plugin-base-bom:$moengagePluginBaseBomVersion"))
    api("com.moengage:plugin-base-personalization:0.0.2-SNAPSHOT")
    compileOnly("com.moengage:plugin-base")

    implementation(platform("com.moengage:android-bom:$moengageNativeBomVersion"))
    compileOnly(platform("com.moengage:android-bom:$moengageNativeBomVersion")) {
        exclude group: 'com.moengage', module: 'core'
    }
    compileOnly("com.moengage:moe-android-sdk")
    api("com.moengage:personalization-core")
}
