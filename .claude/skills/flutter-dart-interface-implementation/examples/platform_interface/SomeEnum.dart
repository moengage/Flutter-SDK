/// <EnumName> — derived from protos/<contractDir>/<ProtoFile>.proto
enum <EnumName> {
  /// <description of value one>
  <valueOne>,

  /// <description of value two>
  <valueTwo>,

  // add one value per proto enum value
}
