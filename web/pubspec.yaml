name: moengage_flutter_web
version: 0.0.1

flutter:
  plugin:
    platforms:
      web:
        pluginClass: MoEngageFlutterPlugin
        fileName: moengage_flutter_web.dart

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

environment:
  sdk: ">=2.1.0 <3.0.0" 
  